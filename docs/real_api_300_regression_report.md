# 300-Problem Real API Regression Report

## Summary

This report records the real Intern-S1 API regression run for the hard official-style suite generated by `scripts/generate_hard_hidden_math.py --profile large`.

- Dataset: `data/hard_official_math300.jsonl`
- Answers: `data/hard_official_math300_answers.jsonl`
- Generated suite size: 360 problems
- Real API run size: 300 problems
- Coverage: 100 proof, 100 number theory, 100 geometry
- Raw trace/results location: local `audit-temp/benchmark_real_hard_official_300`
- Raw traces/results are intentionally not committed.

## Command

```powershell
python scripts/run_benchmark_suite.py `
  --input data\hard_official_math300.jsonl `
  --answers data\hard_official_math300_answers.jsonl `
  --out-dir ..\audit-temp\benchmark_real_hard_official_300 `
  --limit 300 `
  --real `
  --enable-tools `
  --mode-pattern fast,tool-first,tool-first `
  --ab-limit 0
```

The API key was supplied only through a transient environment variable and was not written to repository files.

## Final Metrics

Final evaluation was regenerated after improving the proof rubric to recognize theorem-substitution proof chains such as "use theorem, substitute, simplify, therefore". No extra API calls were needed for that re-evaluation.

| Metric | Result |
|---|---:|
| Total | 300 |
| JSON valid | 300 |
| Success | 300 |
| Partial | 0 |
| Fail | 0 |
| Evaluation pass | 300 |
| Evaluation pass rate | 1.0000 |
| Trace coverage | 300 |
| Model calls | 100 |
| Tool calls | 500 |
| Wall time | 3900.09 s |
| Average time/problem | 12.99 s |

## Accuracy By Domain

| Domain | Total | Result |
|---|---:|---:|
| Proof | 100 | 100/100 |
| Number theory | 100 | 100/100 |
| Geometry | 100 | 100/100 |

## Short Answer Metrics

| Metric | Result |
|---|---:|
| Covered short-answer items | 200 |
| Exact match | 1.0000 |
| Normalized match | 1.0000 |
| Numeric match | 0.9000 |
| Symbolic match | 1.0000 |

The lower numeric-match rate is caused by radical chord-length answers such as `2*sqrt(n)`. They match symbolically and exactly after normalization.

## Proof Quality Metrics

| Metric | Result |
|---|---:|
| Proof items | 100 |
| Proof quality pass | 100 |
| Proof quality rate | 1.0000 |
| Average proof score | 0.9991 |
| Proof complete | 100 |
| Proof partial | 0 |
| Proof invalid | 0 |
| Proof risk counts | none |

## Failure Replay

Final replay report:

- Failure count: 0
- Failure categories: none

Local artifact:

```text
C:\Users\y\Documents\Codex\2026-05-27\files-mentioned-by-the-user-xh\audit-temp\benchmark_real_hard_official_300\mixed_official_like\failure_replay_report_rerubric.md
```

## Health Check

Final repository health checks:

| Check | Result |
|---|---|
| `python -m pytest -q` | PASS, 380 passed |
| `ruff check . --no-cache` | PASS |
| `black --check src scripts demo tests` | PASS |
| `isort --check-only src scripts demo tests` | PASS |
| `mypy src --show-error-codes` | PASS |
| `pyright src scripts` | PASS |
| `python -m compileall -q src scripts demo tests` | PASS |
| `scripts/check_project_safety.py` | PASS |
| `git diff --check` | PASS, line-ending warnings only |

## Submission Judgment

Can submit: yes.

Blocking issues: none found after the 300-problem real API regression and full health check.

Residual risk:

- The 300-problem suite is synthetic official-style coverage, not the real hidden set.
- Proof quality scoring is heuristic, although now stricter than proof validity and capable of catching partial proof shells.
- The API key appeared in chat history outside the repository. Rotate it before official use.

## Recommendation

This revision is suitable to push to GitHub. It should not include raw outputs, trace JSON, cache directories, `.env`, API keys, or submission archives.
