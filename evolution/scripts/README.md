# Scripts (Offline)

Reserved for offline helper scripts such as:
- manifest validation;
- candidate metric aggregation;
- regression gate checks.

Scripts here must not mutate stable pipeline runtime behavior by default.
