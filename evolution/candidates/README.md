# Candidates

This folder hosts offline candidate metadata and comparison artifacts.

- `index.example.json` is a schema example for candidate tracking.
- Candidate promotion must satisfy regression gates before freeze.
- Weighted Voting / Budget Scheduler can be used for offline candidate analysis only; do not auto-wire into stable main flow.
