# Intern-S1 Math Agent

This is NOT official evaluation.
Default mode is mock-safe.
official_results.jsonl is never generated by dry-run/demo/audit tools.
Real API calls require explicit real/allow-real gates.
Do not commit .env, outputs, traces, caches.

## Full Function Inventory Overview

P18.5.1 增强了 full system audit 的 A-X 全板块功能盘点（Stable Core、Tools、Formatter、Proof、Skills、Memory、Protocol、Hard-mode、Weighted Voting、Budget、Evaluation、Regression、Debugger、Trace/Replay、Demo、Evidence Pack、Ablation、Dry Run、Submission、Offline Evolution、Safety、Project Health、Docs、Audit 自身）。

## Architecture / Full Chain

CLI/Pipeline → Hard-mode hooks → Candidate budget/routing/voting/proof guard preview → Shadow Eval + Debugger + Ablation → Dry Run → Demo Evidence Pack → Safety & Quality Gates。

## Quality Gates

- ruff / black / isort / mypy / pyright / compileall / pytest / check_project_safety

## Safety Boundaries

- 不读取 .env 内容
- 不真实调用 API（默认 mock-safe）
- 不生成 official_results.jsonl

## Audit Regeneration

```bash
python scripts/full_system_audit.py --out-dir outputs/full_system_audit --skip-slow
python scripts/full_system_audit.py --out-dir outputs/full_system_audit
```

## Current Module Status

- 详见 `outputs/full_system_audit/function_inventory.json`（运行后生成）
- 以及仓库文档 `docs/full_system_audit.md`

## P19 / P20 Roadmap

- P19：增强回归证据和模块化风险收敛
- P20：更强 verifier 与评测链路强化
